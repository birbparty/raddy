## Smoke test: raddy entry point compiles and is importable.
## Grows as submodules are added.

import raddy

# Nothing exported yet — just verifying the module compiles without error.
echo "raddy smoke test: ok"
